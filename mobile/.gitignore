node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Native folders are generated by `expo prebuild` — keep them out of git.
# Run `npx expo prebuild` locally to produce the android/ and ios/ folders.
/android
/ios

.env
.env.local

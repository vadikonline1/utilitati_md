# Copy to .env and adjust values before running `docker compose up -d`.
# Generated by docker compose automatically (no quotes needed).

UTILITATI_SECRET_KEY=change-me-in-production
UTILITATI_USERNAME=admin
UTILITATI_PASSWORD=admin

# Optional email (SMTP) configuration
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=no-reply@example.com
# SMTP_PASS=your-smtp-password
# SMTP_FROM=no-reply@example.com

# Optional Telegram bot configuration
# TELEGRAM_TOKEN=123456:ABC-DEF...
# TELEGRAM_BOTNAME=utilitati_md_bot
